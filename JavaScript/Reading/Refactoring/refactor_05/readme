`提炼变量` `提炼函数`
`内联变量` `内联函数`
`改变函数声明` `变量改名` `变量封装` `引入参数对象`
`函数组合成类`  `函数组合成变换`
`拆分阶段`

提炼函数：将一段需要理解的代码进行提炼，并以其作用命名。写非常小的函数，每个函数代码建议在6行之内

提炼变量：引入解释性变量。1. 表达式复杂 2. 重复引用

内联函数：分解函数。当函数内容足够简单清晰的时候

内联变量：删除足够简洁，清晰的变量。使用内联变量替代