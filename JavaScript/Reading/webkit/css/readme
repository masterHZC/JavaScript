CSS解释器和样式布局
CSS解释器的处理阶段在，DOM构建之后以及RenderObject生成之前。RenderObject会基于该结果来进行规则匹配和布局计算。
CSS主要的特征是将 `网页展示的内容和展示的方式分离`

样式规则：
  一个规则包含连个部分：规则头和规则体。`规则头`由选择器构成，`规则体`由一个或者多个样式组成，`样式`由样式名和样式值组成

盒子模型：
  盒子模型是CSS标准中引入来表示HTML标签的布局结构。一个盒子模型包括四部分：外边距（margin）、内边距（padding）、边框（border）、内容（content）。

CSSOM
  CSS Object Model。它的思想是在DOM中的一些节点接口中，加入获取和操作CSS属性或者接口的JS接口，为JS提供动态操作CSS的能力。DOM提供接口让JS修改HTML，CSSOM提供接口让JS修改CSS
