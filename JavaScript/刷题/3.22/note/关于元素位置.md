# 关于元素位置、元素的大小
```
scrollTo, scrollBy
scrollTop, scrollLeft
scrollHeight, scrollWidth
pageXOffset, pageYOffset
clientHeight, clientWidth
offsetHeight, offsetWidth, offsetParent
getBoundingClientRect
IntersectionObserver
```
##
```
window.
```